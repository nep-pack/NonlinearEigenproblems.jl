close all
clear all
clc

T=@(i,x) cos(i*acos(x));
a=-1;   b=1;
c=(a+b)/(a-b);

N=10;
n=20;

y=rand(n,N);

y0=zeros(n,1);
for i=1:N
    y0=y0+T(i,c)*y(:,i);
end
y0=-y0;

k=2/
for i=0:N-1
    y0=y0+T(i,-k*tau+c)*x(:,i)
end